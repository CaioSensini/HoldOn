{
  "name": "float",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Float — endless runner mobile (HTML5/TypeScript/Phaser 3)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.10"
  }
}
